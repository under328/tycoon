# 路线图 · 下一阶段待办（架构师视角）

> 更新: 2026-09-14 · 基于 v1.2 规划（本地普通/肉鸽/格斗试炼/每日挑战 + 联机
> 普通/肉鸽/格斗对战 + 经济闭环 + 留存深化: 成就II/命运卡图鉴/统计总览）

## 本期已交付（v1.2 · 留存深化与目标感）

- [x] 成就 II: 目录 16→23, 修复失效项(fight_10 旧无尽层数语义永不可解锁;
  fight_bosses 计数悬空), 新增格斗通关/每日挑战/肉鸽胜场/命运卡全收集/
  PvP 首胜共 8 项
- [x] 格斗每日挑战: 日期数字种子(全设备同一天同一布局), 主菜单模式弹窗入口,
  每日最佳(回合优先/剩余生命次序)持久化, 结算面板对比"新纪录"
- [x] 命运卡图鉴: 出现/选用计数(选用不重复计出现), rogue_help 新增进度页,
  见齐 10 张即时解锁"命运收藏家"
- [x] 档案面板第 4 页签「统计」: 大富豪/肉鸽/格斗/每日挑战/PvP/图鉴 总览;
  顺带修复"每日任务"页签不高亮
- [x] 局数计数补全: fight_runs/fight_bosses 之前从未自增(旧版遗留),
  肉鸽场次/胜场接入 grant_match_reward(新 mode 参数)

## 已完成基线（不重复展开）

- 规则引擎：大富豪全规则 + 肉鸽命运卡（引擎层，可重放）
- 联机：Tailscale + 局域网 UDP 发现 + 多地址邀请码；专用/内嵌双服务器形态
- 联机模式：普通 + 肉鸽（固定剧本）+ **格斗对战**（房间 mode=fight：前 2 座
  回合制 1v1，3/4 座观战，先胜 3 回合获胜；1 人开局自动补 AI）
- 格斗试炼 v2：回合制二选一抽牌 + 5 槽装备 + 8 种奇物 +
  小怪/精英/BOSS 日程 + 三主题像素怪群（本地单人，与联机同源规则）
- 经济与留存：金币/钻石、装扮商城、双倍钻石卡、每日首胜、每日签到、成就(14)、
  对局记录(20 条)、称号
- 质量：零依赖测试框架(10 万+断言)、自适应布局普查、E2E 双链路、双端导出

## 发布就绪 v1.2.0（本期已交付）

- [x] 首启语言跟随系统(OS locale 检测, zh→中/en→英/日韩西等→对应语言)
- [x] 高频动态文案 tr() 迁移: 格斗引擎战报/竞技场状态/每日挑战结算/
  档案统计与战绩头 ~55 条模板(en/繁/日/韩全量, 核心语言回退英语)
- [x] 格斗音效: 新增 hit/crit/hurt 合成音效并映射命中/暴击/受击
- [x] i18n 漂移审计工具 tools/i18n_audit.py(未覆盖清单 + 失配键检测,
  当前覆盖率 43%, 未覆盖项主要为帮助页长文与深层数据文案)
- [x] 每日挑战文案统一(不限次挑战, 取当日最佳); 版本号 1.1.0→1.2.0

## 下一期候选（按价值排序）

1. 服务器权威经济（见 P1）— 每日挑战榜单/赛季的前提
2. 对局回放（见 P2）
3. 联机肉鸽真草稿: 现为固定剧本(服务器预选), 可升级为双方各自二选一
4. 格斗模式内容厚度: 第 4/5 主题怪群、BOSS 专属技能、奇物扩充

## P1 · 服务器权威经济（跨设备钱包）

现状钱包为本地存档（可改档、换设备丢失）。方案：
1. 阶段一：本地钱包 + 云备份码（导出/导入 JSON+签名校验），零服务器改动。
2. 阶段二：dedicated server 挂 `--wallet-db user://wallets.db`（SQLite 或 JSON 分片），
   客户端以 client_id 幂等上报对局结果，服务端复算积分发放（防作弊关键：
   服务端只信任自己的 MatchController 结算，不信任客户端上报的分数）。
3. 迁移：首登本地钱包快照上云，之后以云端为准、本地做缓存。
风险：中（需要迁移与幂等设计）。

## P2 · 对局回放

基础已具备：`seed + cfg` 可完整重放任何本地局（`_deal_round` 按局定随机）。
方案：对局记录条目扩容 `{seed, cfg, actions: [...]}` 落盘；
回放器 = 逐条 `GameState.apply` + table 只读渲染（禁操作）。
工作量：1-2 个会话；可复用格斗对战的观战广播路径（s_fight_state）。

## P4 · 成就 II / 赛季

- 成就 II：肉鸽专项（抽到每种命运卡至少一次 / 肉鸽胜场 / 双倍赌局翻盘），
  需要本地统计新增 rogue 维度计数（当前 grant 不区分模式 → 需在
  `push_history` 之上加统计聚合，或 grant 增加 meta 参数）。
- 赛季：称号按月重置 + 赛季徽章；依赖服务器权威经济(P1)才有公信力，建议 P1 后。

## P4 · 帮助页与内容数据国际化（人工翻译量大）

- 帮助页(rogue/normal/fight/lobby)长文本、命运卡/奇物/怪物名等
  内容数据仍为中文 — 词典与 auto-translate 管线已就绪, 补词条即可;
  建议委托人工翻译后填入 tools/i18n_data_*.py 再运行 i18n_build.py

## P5 · 技术债

- `table.gd`（1300+ 行）本地/联机双路径耦合：建议抽 `LocalMatchDriver` 与
  `OnlineMatchDriver` 两个策略类，table 只留渲染与输入（重构窗口：
  无新功能的一个会话，靠现有 10 万断言兜底）。
- `lobby.gd`（1100+ 行）入口/房间双视图 + 局域网发现 + 邀请码：同类拆分。

## 明确不做（记录原因）

- 账号密码体系：目标场景是亲友局，client_id + 云备份码已覆盖，微信/QQ 登录
  引入审核与合规成本，收益不匹配。
- 排位匹配（ELO）：在线人数不足时匹配体验差，待联机规模起来再评估。
